import Vue from 'vue'
import Router from 'vue-router'
import Cards from '@/components/cards'

Vue.use(Router)

export default new Router({
  routes: [
    {
      path: '/',
      name: 'HelloWorld',
      component: Cards
    }
  ]
})
